/**
 * Những gì không đổi theo ngôn ngữ: thương hiệu, địa chỉ liên hệ, URL và màu
 * sắc của các hành tinh. Để chung một chỗ để hai bản ngôn ngữ không trôi dạt
 * khỏi nhau.
 */
export const emails = ['binean.enova@gmail.com', 'ingenium.modernization@gmail.com'];
export const mail = `mailto:${emails[0]}`;

export const brandName = 'BENOVA';
export const siteUrl = 'https://binean.com/';
export const themeColor = '#0A192F';

/** Màu và vành của từng hành tinh — thuộc về hình ảnh, không phải ngôn ngữ. */
export const planetVisuals = {
  E: { color: '#5eead4' },
  N: { color: '#fbbf24' },
  O: { color: '#a78bfa', ring: true },
  V: { color: '#fb7185' },
  A: { color: '#a3e635' },
};
